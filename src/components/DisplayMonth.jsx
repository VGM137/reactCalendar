import React from "react";
import { connect } from "react-redux";
import '../assets/styles/components/DisplayMonth.scss'

const DisplayMonth = () => {
  return(
    <div id='display-month' className='display-month'>
      
    </div>
  )
}

export default connect(null, null)(DisplayMonth)